let Koa,Router,bodyParser,mongoose,api,createFakeData;_312‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_312‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_312‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_312‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_312‍.w("./api",[["default",["api"],function(v){api=v}]]);_312‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);require('dotenv').config();







const { PORT,  MONGO_URL } = process.env;

mongoose
  .connect(
    MONGO_URL,
    { useNewUrlParser: true, useFindAndModify: false, useUnifiedTopology: true  })
  .then(() => {
    console.log('Connected to MongoDB');
    createFakeData();
  })
  .catch(e => {
    _312‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// apply bodyParser before applying router
// put JSON type of data in request body and parse it for a server to use
app.use(bodyParser());

router.use('/api', api.routes()); // apply api route

// apply router in app instance
app.use(router.routes()).use(router.allowedMethods());

// If port is not assigned, then use 4000
const port = PORT || 4000;

app.listen(port, () => {
  _312‍.g.console.log('listening on port %d', port);
});